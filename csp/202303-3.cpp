#include <bits/stdc++.h>
using namespace std;
#define MAX_SIZE 1000000001
map<int, map<int, int>> attrubute;
map<string, bool> expression(string expr)
{
    map<string, bool> retMap;
    queue<string> que;
    string str;
    
    

    return retMap;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    
    return 0;
}